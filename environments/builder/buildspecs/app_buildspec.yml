version: 0.2

phases:
  install:
    commands:
      - echo "Install App"
  pre_build:
    commands:
      - echo "Pre_build App"
  build:
    commands:
      - ./gradlew clean test bootJar
  post_build:
    commands:
      - echo "post_build App"